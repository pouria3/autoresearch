# Bumblebee Prompt Research Summary

Use this file as the running lab notebook.

For each experiment, record:
- query or cluster of queries tested
- what failed in the live baseline behavior
- hypothesis
- prompt changes made in `winning_prompt.txt`
- whether the change looks promising, risky, or unproven

Do not ask the human to patch glue files. Capture the live baseline prompt from `bumblebee_debug.prompt`, write the improved draft to `winning_prompt.txt`, and keep going.
